# SSO-only lab cleanup — 22 September 2026

The reviewed Terraform plan was applied: five obsolete member-account IAM roles
and their five inline policies were removed.

- `bedrock-cost-lab-alice-a`
- `bedrock-cost-lab-alice-b`
- `bedrock-cost-lab-bob-a`
- `bedrock-cost-lab-untagged`
- `bedrock-cost-lab-session`

Each role had only its expected `bounded-bedrock-inference` inline policy, no
attached managed policies and no instance profiles. No member role trust policy
referenced these callers. The earlier tests last used them before the cleanup.

## Current configuration

`bedrock-lab/` remains an empty retirement root so existing deployments can
apply the same removal. Its declarations cannot recreate the retired roles.
The active `artifacts/test-config.json` is now exported from
`sso-lab`'s `sso_test_config` output, including account and model settings.
The SSO policy uses those same model constants.

`scripts/configure.py` prepares the independent SSO inputs/backend.
`scripts/sso_idp.py` no longer reads or copies the retired root's configuration.
Legacy invocation and validation commands reject the SSO configuration rather
than treating an empty set of callers as a successful test.

## Verification

- All six Terraform roots report no changes after apply.
- The five legacy roles are absent.
- Every remaining member role retained its ID, trust document, inline policies
  and managed policy attachments.
- The shared `BedrockCostLab` SSO role still has three user assignments.
- IAM policy simulation allows the intended Runtime and Mantle model calls.
- Both IAM-principal cost-allocation tags remain active.
- Six sandbox SAML login checks passed for the three users and two clients.
- Twelve Python tests passed, including guards against empty legacy validation.

The cleanup made no Bedrock inference calls. These checks establish retained
configuration and signed IdP behavior; they do not claim a new AWS browser
session or a new SSO billing result.

## Preserved evidence and infrastructure

Before deletion, the direct-session experiment's delivered CUR report was
refreshed. All eight Runtime/Mantle caller cases passed exact-session attribution
and expected-tag validation. This is the earlier direct STS experiment, separate
from validating the Identity Center SSO path.

Private evidence is retained under `artifacts/legacy/` and
`artifacts/cleanup-2026-09-22/`, including the historical caller configuration,
refreshed report, original Terraform state, reviewed plans and post-check result.
The invocation ledger and original billing objects remain.

State/report storage, CUR export, budget, principal-tag activations, SSO users,
permission set, account assignments, administrator access, organization access,
service-linked roles, both accounts, the organization and the Mantle default
project were retained. The shared sandbox Keycloak realm was not changed.

This cleanup covers the obsolete lab resources; it does not claim an exhaustive
inventory or deletion of unrelated resources throughout both AWS accounts.
